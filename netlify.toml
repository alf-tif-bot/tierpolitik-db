# Legacy compatibility only.
# Runtime API is now domain-native on monitor.tierimfokus.ch via same-origin endpoints.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
