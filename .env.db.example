# PostgreSQL Verbindung für DB-Migrationspfad
# Für lokale Entwicklung z.B. mit Docker/Postgres lokal.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/tierpolitik_vorstoesse

# Optional: falls SSL benötigt wird (z.B. managed DB)
# PGSSLMODE=require
